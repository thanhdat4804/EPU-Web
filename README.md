# Lệnh chạy server blockchain

npx hardhat node

# Lệnh chạy file deploy.js(sau khi chạy server blockchain)

npx hardhat run scripts/deploy.js --network localhost

# Lệnh chạy server backend

npm run start:dev

# Lệnh chạy server frontend

npm run dev

